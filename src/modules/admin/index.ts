/**
 * Admin Console module: dashboard, permissions, game control, ledger review, audit.
 * Routes live under `src/app/admin`. Never reuse player session assumptions here.
 */
export const ADMIN_MODULE = "admin" as const;
